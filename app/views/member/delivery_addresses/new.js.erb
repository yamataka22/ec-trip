var modal = $('.address_form_dialog');
modal.find('.modal-content').html("<%= escape_javascript(render 'new', address: @address) %>");
if (modal.css('display') == 'none') {
    modal.modal({
        backdrop: 'static'
    });
}